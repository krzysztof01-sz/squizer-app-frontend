import Layout from '../../global/Components/Layout';
import Form from './Form/index';

const SignupForm = () => {
  return (
    <Layout>
      <Form />
    </Layout>
  );
};

export default SignupForm;
