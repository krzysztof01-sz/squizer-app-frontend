import Layout from '../../global/Components/Layout';
import Form from './Form';
import './index.scss';

const QuizForm = () => {
  return (
    <Layout>
      <Form />
    </Layout>
  );
};

export default QuizForm;
